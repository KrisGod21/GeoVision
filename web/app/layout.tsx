import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "GeoVision — AI Feature Extraction from Drone Orthophotos",
  description:
    "Extracts building footprints with roof classification, roads, and water bodies from SVAMITVA drone orthophotos. SIH 2026, Problem Statement 1705.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
